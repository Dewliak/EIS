# EIS — combined dependencies
# EUDI login service (FastAPI) + Streamlit apps (login_app.py, webapp/app.py)

# Backend verifier service — eudi_login/service.py
fastapi>=0.141.1
uvicorn>=0.52.4
pydantic>=2.13.4
python-multipart>=0.0.32   # FastAPI Form data
pyjwt>=2.13.0
qrcode>=8.2                # SVG factory used; Pillow/[pil] not required

# Streamlit front-ends — login_app.py, eudi_login/client.py, webapp/app.py
streamlit>=1.62.0
requests>=2.34.2
